---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"li"> & {
  children: any;
};

const { class: className, ...rest } = Astro.props;
---

<li class={className} {...rest} data-slot="pagination-item">
  <slot />
</li>

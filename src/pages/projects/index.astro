---
import ProjectCard from "@/components/project-card.astro";
import Button from "@/components/starwind/button";
import BaseLayout from "@/layouts/base-layout.astro";
import { loadProjectsData } from "@/lib/data-loaders/projects";

const { projects } = await loadProjectsData();
---

<BaseLayout
  title="Projects - ARV"
  description="See all the project created by ARV."
  nav={{ sticky: true }}
>
  <h1 class="font-heading text-3xl font-bold md:text-center mb-6">Projects</h1>
  <div class="grid gap-4 md:grid-cols-2">
    {projects.map((project) => <ProjectCard project={project} />)}
  </div>

  <Button
    class="text-center mt-5 mx-auto w-fit block h-auto py-1.5 px-12"
    size="lg"
    href="/projects/all"
  >
    See All Projects
  </Button>
</BaseLayout>
